import React from 'react';

const Header = () => {
    return (
        <header id="Header">
            <nav>
                <p>Header</p>
            </nav>
        </header>
    );
}

export default Header;
